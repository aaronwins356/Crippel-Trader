# Croc-Bot runtime requirements (Windows-friendly)

# FastAPI backend
fastapi>=0.111
uvicorn>=0.30
orjson>=3.9
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27
websockets>=12.0
aiosqlite>=0.19
structlog>=24.1
numpy>=1.26
pytz>=2024.1

# Dashboards & client utilities
streamlit>=1.33
pandas>=2.2
plotly>=5.20
requests>=2.31

# Optional optimisations (automatically skipped on Windows)
uvloop>=0.19 ; platform_system != "Windows"
watchfiles>=0.21 ; platform_system != "Windows"

# --- Optional local LLM extras ---
# Install crippel-trader/requirements-localllm.txt to enable offline models.
